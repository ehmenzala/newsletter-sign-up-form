{
  "scripts": {
    "sass:watch": "sass --watch src/sass:src/css",
    "test": "echo \"Error: no tests found\" && exit 1"
  },
  "devDependencies": {
    "prettier": "3.0.1",
    "sass": "1.65.1"
  }
}
